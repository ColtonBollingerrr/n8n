# --- Core/URLs ---
N8N_ENCRYPTION_KEY=replace-with-strong-random   # encrypts credentials
N8N_EDITOR_BASE_URL=https://<your-cloud-run-url>  # set after first deploy
WEBHOOK_URL=https://<your-cloud-run-url>          # required for webhooks behind proxy

# --- DB (PostgreSQL is recommended in production) ---
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=<cloud-sql-ip-or-private-dns>
DB_POSTGRESDB_PORT=5432
DB_POSTGRESDB_DATABASE=n8n
DB_POSTGRESDB_SSL_ENABLED=true

# --- Binary data mode ---
# Default keeps binaries in memory. 'filesystem' needs persistent disk (Cloud Run doesn't have it).
# S3 external storage is enterprise-only.
# N8N_DEFAULT_BINARY_DATA_MODE=default

# --- Misc limits (tune as needed) ---
N8N_PAYLOAD_SIZE_MAX=32
N8N_FORMDATA_FILE_SIZE_MAX=512
